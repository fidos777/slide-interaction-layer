# P4 — Modal (Deep Dive) — USE SPARINGLY
Use for **one optional deep dive** (case study, long quote, detailed chart) per slide.
**Hard cap: rare.** Never a default. Esc closes, focus moves to dialog, click-outside closes.
Fallback: keep the summary on the slide; append deep-dive content below.
