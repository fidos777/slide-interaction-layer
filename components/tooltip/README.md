# P3 — Tooltip / Glossary
Use to define **a few inline terms** without leaving the flow. Works on hover, tap, and keyboard.
**Avoid** when the definition is essential — put it on the slide instead. Completion: none.
Fallback: definition text is in the DOM and readable when forced open.
