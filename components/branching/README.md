# P7 — Branching Scenario
Use when the viewer must **decide and see a consequence** (roleplay, "what would you do").
**Avoid** when there's one correct linear path. Completion: `reached_an_ending`.
Fallback: a linear summary of the recommended path.
