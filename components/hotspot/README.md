# P5 — Hotspot Discovery
Use to **explore parts of an image/diagram/map/screenshot** one at a time.
**Avoid** for purely linear content. Completion: `all_hotspots_clicked` (great learning gate).
A11y: hotspots are `<button>`s with focus rings. Fallback: labeled static diagram.
