# P2 — Accordion
Use for **layered/optional detail** or FAQ where the viewer self-selects depth (4–6 items).
**Avoid** when every item must be read. Completion: `at_least_one_opened`.
A11y: `<button>` headers, `aria-expanded`, keyboard operable. Fallback: open all panels.
