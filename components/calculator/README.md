# P8 — Calculator / Simulation
Use for **personalized math**: ROI, savings, pricing, time saved, payback.
**Avoid** when a single static number tells the story. Completion: `produced_a_result`.
Fallback: a worked example with default numbers is shown on load.
