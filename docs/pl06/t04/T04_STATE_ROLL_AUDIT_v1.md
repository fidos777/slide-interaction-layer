<!-- GENERATED by docs/pl06/t04/tools/t04_state_emit_v1.py — Stage 4.2F-B1.1. Edit the model, not this file. -->

# T04 — FROZEN STATE ROLL AUDIT

```
STAGE    = 4.2F-B1.1
SUITE_ID = T04_STATE_COVERAGE_QA_v1
STATUS   = T04_LAYOUT_STORYBOARD_BUILT
DECLARED_TOTAL_RUNTIME_STATES = 65
LIVE_STATE_COUNT              = 65
FROZEN_PANEL_STATES           = 52
FROZEN_FULL_PAGE_STATES       = 13
PAGES_FROM_THE_FROZEN_ROLL    = 28
PANEL_BUDGET                  = 2
MEASURED_RENDERER_CAPACITY    = 4
```

Mutation fixture A-03 deleted one runtime state and rebuilt the appendix. Every appendix gate still passed: the page plan and the deck were both projected from the mutated inventory, so they agreed with each other about a state that no longer existed. The roll below is the reviewed inventory written down by hand and never derived from `states()`, so a state that leaves the model now fails a gate.

Fixture A-01 exposed the same shape in the panel budget: the gate compared pages built from `PANELS_PER_PAGE` against `PANELS_PER_PAGE`. The budget is now checked against a literal, and separately against a capacity measured by laying every panel out at each candidate width — 4 panels fit before anything overflows, so the two-panel rule is a review-legibility policy, stricter than the renderer.

| Check | Result |
|---|---|
| Live inventory equals the frozen roll | yes |
| Missing from live | none |
| Added to live | none |
| Duplicate live IDs | none |
| Frozen panel states missing from the plan | none |
| Planned states not on the frozen roll | none |
| The two rolls are disjoint | yes |
| The two rolls cover the state roll | yes |
