<!-- GENERATED by docs/source/tools/src_k2f_emit_v1.py — Stage 4.2F-F. Edit the model, not this file. -->

# K2 binary custody record — v2 (Stage 4.2F-F)

```
STATUS           = BINARY_LOCAL_HASH_VERIFIED
BINARY_LOCAL     = True
BINARY_READABLE  = True
CONTENT_READ     = FULL
EVIDENCE_CLASS   = LOCAL_BINARY_HASH_VERIFIED
LOCAL_PATH       = .source-intake/K2/2. PENGURUSAN PEMBINAAN LANDASAN.pdf
```

## The status moved because it is finally true

Claimed now: `SOURCE_BINARY_LOCAL`, `SOURCE_BINARY_READABLE`, `SOURCE_CONTENT_READ`, `BOUNDARY_CONFIRMED`

Each is now true and checkable. The bytes are on disk; their SHA-256 equals the brief's; the full text extracts at 481,659 characters (7.7x the truncated connector read); and every PL package boundary is confirmed twice over — by a clean 346-page partition with no gaps or overlaps, and by each package's own declared 'Muka Surat 1/N' length.

Still not claimed: `EXTRACTED_ALL_NINE`, `MODELLED_ALL_NINE`, `UNIT_GRANULARITY_DECIDED`

Content is read for the whole document, but only two packages (PL07, PL09) were carried through a controlled extraction and model this run — the brief asked for the first two, not all nine. And whether one PL equals one storyboard unit is an instructional decision, not a measurement; it is raised in the K2 decision register, not settled here.

## Identity, verified from the bytes before anything was measured

| field | expected | observed | match |
|---|---|---|---|
| bytes | 25043306 | 25043306 | True |
| sha256 | 0b2d81dda46eaf4f… | 0b2d81dda46eaf4f… | True |
| pages | 346 | 346 | True |

| check | result |
|---|---|
| PDF validity | OPENED_AND_PARSED_BY_PYMUPDF |
| encryption | NOT_ENCRYPTED |
| needs password | False |
| PDF format | PDF 1.7 |
| author | Ts. Faezal Husni |
| creator | Adobe Acrobat Pro DC 20.6.20042 |

## Binding

`K2-BIND-02` supersedes `K2-BIND-01 (CONNECTOR_IDENTIFIED)`.

The local binary hashes to exactly the SHA-256 the brief stated. The identifier, the byte count and now the content hash all agree, so the binding moves from CONNECTOR_IDENTIFIED to LOCAL_BINARY_HASH_VERIFIED. The two-files-can-share-a-size caveat from 4.2F-E no longer applies: a shared SHA-256 is not a coincidence.

## Relationship to the earlier record

Supersedes: `K2-CUSTODY-02 (Stage 4.2F-E connector-identified record)`

K2-CUSTODY-02 is PRESERVED_UNCHANGED as historical evidence. It was correct when written: on that box the binary was genuinely absent and the status was rightly held at NOT_FOUND_IN_LOCATIONS_SEARCHED. This record does not overturn it — it records a later, different fact: the binary is now on local disk and its identity matches the brief, so the states 4.2F-E refused to claim are now claimed, on evidence.
