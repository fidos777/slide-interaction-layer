# SOURCE_DEFECT_REGISTER

```
STATUS: BLOCKED — NOT POPULATED
```

**No content has been entered in this document.** The module PDF
(`[PROOFREAD FINAL] SKP 2025 PEMBINAAN LANDSKAP LUAR 300426_compressed.pdf`) is **absent from this
session** — see `SOURCE_ARTIFACT_INVENTORY.md` §1.2 for the searches run.

Every field below must be **read from the source**. Filling it from inference would produce a
source-completion record that is not source-bound, which is the one failure this gate exists to
prevent. The schema is fixed here so that intake is a single pass once the PDF arrives.

---

## Pre-registered items — **reported, not observed**

These four were named in the request. **None has been located, quoted or verified** — that requires the
PDF. They are recorded here so intake checks them first.

| # | Item | Status | To determine |
|---|---|---|---|
| **D-1** | `Promenande` / `Promenade` variant | **reported — unverified** | which spelling appears, where, how many times, which is intended; treatment in display and VO |
| **D-2** | repeated `3.4.1` numbering | **reported — unverified** | which headings collide; numbering slip or two genuine subsections; effect on the S11–S15 mapping |
| **D-3** | English-origin term styling | **reported — unverified** | which English-origin terms appear in scope; whether the module already styles them; whether the 3-term lexicon needs extending |
| **D-4** | `reka bentuk` / `rekabentuk` variants | **reported — unverified** | which form dominates; whether both appear in scope; which to carry into display and VO |

**`reported — unverified` is doing real work here.** These are Firdaus's observations of the module,
recorded faithfully. They are not measurements, and this register does not present them as such until
each carries a page reference and a quotation.

## Schema — one row per defect, once verified

| ID | type | physical page | module page | quotation (verbatim) | classification | proposed treatment | affects |
|---|---|---|---|---|---|---|---|
| *(pending source)* | | | | | | | |

Classifications: `source typo` · `numbering defect` · `terminology variant` · `styling inconsistency` ·
`unresolved source variant`.

## Carried forward from earlier gates — already measured

| Item | Standing |
|---|---|
| `BBQ pit` vs `BBQ Pit` | source form measured as **`BBQ pit`**, lowercase `p`. Recasing is an `OPEN_DECISION` on normalisation policy, not a defect |
| `IMG-01` ms 237 on a `Papan Tanda` screen | Bariah-introduced provenance regression; corrected to `IMG-05` ms 243 in the sample |
| `dan` → `Dan` over-capitalisation | corrected in the sample |

The normalisation-policy question — does list-internal consistency override exact source reproduction
for proper-noun furniture labels — governs `BBQ pit` and three sibling rows **together**, and is for a
human. The module may inform it; it does not settle it.
